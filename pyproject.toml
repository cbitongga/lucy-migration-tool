[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "migration-workbench"
version = "0.2.0"
description = "Evidence-led migration ledger and Copilot/Claude Code tools for C# to Java/React modernization"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
modernizer = "modernizer.__main__:main"

[tool.setuptools]
packages = ["modernizer"]
